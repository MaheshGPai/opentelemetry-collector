diff a/config/confighttp/server.go b/config/confighttp/server.go	(rejected hunks)
@@ -8,6 +8,7 @@ import (
 	"crypto/tls"
 	"errors"
 	"fmt"
+	"golang.org/x/net/netutil"
 	"io"
 	"net"
 	"net/http"
@@ -27,6 +28,7 @@ import (
 	"go.opentelemetry.io/collector/config/configauth"
 	"go.opentelemetry.io/collector/config/configcompression"
 	"go.opentelemetry.io/collector/config/confighttp/internal"
+	"go.opentelemetry.io/collector/config/confighttp/mw"
 	"go.opentelemetry.io/collector/config/configopaque"
 	"go.opentelemetry.io/collector/config/configtelemetry"
 	"go.opentelemetry.io/collector/config/configtls"
@@ -333,6 +335,13 @@ type ServerConfig struct {
 	// is zero, the value of ReadTimeout is used. If both are
 	// zero, there is no timeout.
 	IdleTimeout time.Duration `mapstructure:"idle_timeout"`
+
+	// MiddlewareTimeout is the maximum duration after which the client request gets a timeout response.
+	// MiddlewareTimeout duration starts after the headers are read
+	MiddlewareTimeout time.Duration `mapstructure:"mw_timeout"`
+
+	// MaxActiveConnectionCount is the maximum simultaneous connections to be accepted.
+	MaxActiveConnections int `mapstructure:"max_active_connections"`
 }
 
 // NewDefaultServerConfig returns ServerConfig type object with default values.
@@ -356,6 +365,9 @@ type AuthConfig struct {
 	// RequestParameters is a list of parameters that should be extracted from the request and added to the context.
 	// When a parameter is found in both the query string and the header, the value from the query string will be used.
 	RequestParameters []string `mapstructure:"request_params"`
+
+	// MaxActiveConnectionCount is the maximum simultaneous connections to be accepted.
+	MaxActiveConnections int `mapstructure:"max_active_connections"`
 }
 
 // ToListener creates a net.Listener.
@@ -480,7 +495,7 @@ func (hss *ServerConfig) ToServer(_ context.Context, host component.Host, settin
 	}
 
 	server := &http.Server{
-		Handler:           handler,
+		Handler:           mw.TimeoutHandler(handler, hss.MiddlewareTimeout),
 		ReadTimeout:       hss.ReadTimeout,
 		ReadHeaderTimeout: hss.ReadHeaderTimeout,
 		WriteTimeout:      hss.WriteTimeout,
